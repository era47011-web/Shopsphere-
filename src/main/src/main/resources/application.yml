server:
  port: 8080

spring:
  application:
    name: shopsphere

  datasource:
    url: jdbc:mysql://localhost:3306/shopsphere
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true